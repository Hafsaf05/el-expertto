# El Expertto

El Expertto is a TanStack Start application built with React, Vite, and Tailwind CSS.

## Development

Install the dependencies, then start the development server:

```bash
npm install
npm run dev
```

Create a production build with:

```bash
npm run build
```
